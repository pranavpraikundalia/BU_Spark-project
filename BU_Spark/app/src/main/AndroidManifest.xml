<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pranavraikundalia.bu_spark">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Edu_Activity"
            android:label="@string/title_activity_edu_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Law_Activity"
            android:label="@string/title_activity_law_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Res_Activity"
            android:label="@string/title_activity_res_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_mycase"></activity>
    </application>

</manifest>